class ShortcutOption extends mw.libs.libSettings.Option {
	constructor( config ) {
		config.type = 'Shortcut';
		super( config );
	}

	UI() {

	}

	get UIvalue() {

	}
}

export default ShortcutOption;
